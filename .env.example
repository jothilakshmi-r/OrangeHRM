# Local env variables for running tests locally.
# Copy to `.env` and populate values. Do NOT commit `.env`.
# For CI, configure equivalent secrets in GitHub Actions.
ORANGEHRM_USERNAME=
ORANGEHRM_PASSWORD=
